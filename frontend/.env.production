# Carregado apenas por `npm run build` (o dev server usa .env / o proxy do Vite).
# O Vite injeta este valor no bundle em tempo de build — não é lido em runtime.
# Sem ele o app empacotado resolveria /api contra o próprio WebView.
VITE_API_URL=https://mob-api.brimes.net/api
